# Project-local pi tooling

This directory contains project-local pi extensions and related tooling for working on this NixOS configuration repository.

Pi automatically discovers TypeScript extensions from `.pi/extensions/*.ts` and `.pi/extensions/*/index.ts`. Use `/reload` in pi after editing an extension.
